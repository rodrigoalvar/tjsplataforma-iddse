/**
 * Recepción y Turnero — JS compartido (Fase 0 scaffold).
 */
(function () {
    'use strict';

    window.RecepcionTurnero = window.RecepcionTurnero || {
        version: '1.0.0',
        phase: 0,
    };
})();
